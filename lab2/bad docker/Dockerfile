
#Не рекомендуется использовать последнюю версию, 
#так как после выхода новой докер файл может не работать
FROM python:latest

#Не рекомендуется копировать все, что находится в какой-то папке, 
#так как можно скопировать ненужные файлы, что занимает место, да и вообще зачем они нужны
COPY . /task2/
RUN pip install Flask

#Не рекомендуется оставлять пременную без дефолтного значения, 
#так как, если потом забыть установить значение при работе, 
#то программа будет работать некорректно
#ENV MESSAGE 
ENV MESSAGE="Значение не установлено" 
VOLUME /data/

#Не рекомендуется указывать относительный путь к папке 
WORKDIR ./task2/

#Рекомендуется задавать entrypoint в формате json
ENTRYPOINT python server.py

#Не рекомендуется указывать лишние порты, могут быть проблемы с безопасностью, да и вообще зачем
EXPOSE 80 69 8080
